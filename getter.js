function mapUsers(users) {
  // TODO: replace this
  let hasil = {
    data: users,
    count: users.length,
  };
  return hasil;
}

function artikel(articles) {
  // TODO: replace this
  let hasil = {
    data: articles,
    count: articles.length,
  };
  return hasil;
}

// TODO: answer here
export default mapUsers;
export const mapArticles = artikel;
